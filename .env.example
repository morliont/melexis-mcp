# Atlassian MCP Server - Environment Variables

# Atlassian Instance URL (required)
# This is the URL of your Atlassian Cloud instance
# Example: https://your-domain.atlassian.net
ATLASSIAN_INSTANCE_URL=https://your-domain.atlassian.net

# Atlassian API Token (required)
# Create one at: https://id.atlassian.com/manage-profile/security/api-tokens
ATLASSIAN_API_TOKEN=your_api_token_here

# Atlassian Email (required)
# The email address associated with your Atlassian account
ATLASSIAN_EMAIL=your.email@example.com

# Default Jira Project (Optional)
# If specified, this project will be used as the default for operations
# Example: PROJ
ATLASSIAN_DEFAULT_PROJECT=PROJECT_KEY

# API Version to use (optional, defaults to latest)
# ATLASSIAN_API_VERSION=3

# Gitlab Configuration

# Gitlab instance URL (required)
# Example: https://gitlab.com or your self-hosted instance
GITLAB_INSTANCE_URL=https://gitlab.example.com

# Personal Access Token for authentication (required)
# You can create one at https://gitlab.com/-/profile/personal_access_tokens
# Required scopes: api, read_api, read_repository
GITLAB_ACCESS_TOKEN=your_gitlab_access_token_here

# Optional default Gitlab group to use when not specified
# This is useful if you primarily work with repositories in a specific group
#GITLAB_DEFAULT_GROUP=your_default_group

# Optional API version to use (defaults to v4)
GITLAB_API_VERSION=v4

# Connection timeout in milliseconds (optional, defaults to 10000)
GITLAB_TIMEOUT=10000

# Maximum number of items to return in paginated responses (optional, defaults to 100)
GITLAB_PER_PAGE=100

# Server Configuration
PORT=3000
HOST=localhost

# Logging Level (debug, info, warn, error)
LOG_LEVEL=info 